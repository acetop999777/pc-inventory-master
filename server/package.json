{
  "name": "pc-inv-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "verify": "npm run typecheck --if-present && npm test --if-present && npm run build --if-present",
    "typecheck": "echo \"server: no TypeScript configured; skip typecheck\"",
    "test:ci": "echo \"server: no tests; skip\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
